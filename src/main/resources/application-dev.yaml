spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/magic8ball
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
    platform: postgresql
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: true
server:
  servlet:
    context-path : /task